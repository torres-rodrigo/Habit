<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Tracker.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Tracker"
    xmlns:views="clr-namespace:Tracker.Views"
    Title="Tracker"
    Shell.FlyoutBehavior="Disabled"
    Shell.TabBarIsVisible="True">

    <TabBar>
        <Tab Title="Stats 📊" Route="statistics">
            <ShellContent ContentTemplate="{DataTemplate views:StatisticsPage}" />
        </Tab>

        <Tab Title="Habits ⚛️" Route="habits">
            <ShellContent ContentTemplate="{DataTemplate views:HabitsPage}" />
        </Tab>

        <Tab Title="Tasks 📋" Route="tasks">
            <ShellContent ContentTemplate="{DataTemplate views:TasksPage}" />
        </Tab>
    </TabBar>

</Shell>

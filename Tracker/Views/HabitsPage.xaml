<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Tracker.ViewModels"
             xmlns:controls="clr-namespace:Tracker.Controls"
             x:Class="Tracker.Views.HabitsPage"
             x:DataType="viewmodels:HabitViewModel"
             Shell.NavBarIsVisible="False">

    <Grid>
        <!-- Main Content -->
        <ScrollView>
            <VerticalStackLayout Padding="7" Spacing="15">
                <!-- Show Archived Toggle -->
                <HorizontalStackLayout Spacing="10" HorizontalOptions="End">
                    <Label Text="Show Archived"
                           VerticalOptions="Center"
                           HorizontalOptions="End"
                           FontSize="16"/>
                    <Switch IsToggled="{Binding ShowArchived}"
                            HorizontalOptions="End"/>
                </HorizontalStackLayout>

                <!-- Single List View (when ShowArchived is OFF) -->
                <CollectionView ItemsSource="{Binding ActiveHabits}"
                              SelectionMode="None"
                              IsVisible="{Binding ShowArchived, Converter={StaticResource InvertedBoolConverter}}">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="viewmodels:HabitCardViewModel">
                            <Frame CornerRadius="15"
                                   BorderColor="{Binding HabitColor}"
                                   HasShadow="True"
                                   Margin="0,0,0,5"
                                   Padding="15">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=EditHabitCommand}"
                                                        CommandParameter="{Binding Id}"/>
                                </Frame.GestureRecognizers>

                                <VerticalStackLayout Spacing="8">
                                    <Label Text="{Binding Name}"
                                           FontSize="20"
                                           FontAttributes="Bold"
                                           TextColor="{Binding HabitColor}"/>

                                    <Label Text="{Binding Description}"
                                           FontSize="14"
                                           TextColor="{StaticResource Gray600}"
                                           MaxLines="3"
                                           LineBreakMode="TailTruncation"
                                           IsVisible="{Binding Description, Converter={StaticResource StringNullOrEmptyConverter}}"/>

                                    <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,-5,0,0">
                                        <Label Grid.Column="0"
                                               FontSize="14"
                                               TextColor="{StaticResource Gray600}">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="Week " />
                                                    <Span Text="{Binding WeekNumber}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                        <HorizontalStackLayout Grid.Column="2"
                                                              Spacing="8"
                                                              HorizontalOptions="End"
                                                              Margin="0,0,10,0">
                                            <!-- Circular Progress Indicator -->
                                            <controls:CircularProgressView
                                                Progress="{Binding WeeklyCompletionDecimal}"
                                                ProgressColor="{Binding HabitColor}"
                                                TrackColor="{StaticResource Gray300}"
                                                StrokeWidth="3"
                                                WidthRequest="34"
                                                HeightRequest="34"
                                                VerticalOptions="Center"/>
                                            <Label Text="{Binding WeeklyCompletionPercentage}"
                                                   FontSize="16"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding HabitColor}"
                                                   VerticalOptions="Center"/>
                                        </HorizontalStackLayout>
                                    </Grid>

                                    <!-- Week Progress -->
                                    <CollectionView ItemsSource="{Binding WeekDays}"
                                                  SelectionMode="None"
                                                  Margin="0,-10,0,0">
                                        <CollectionView.ItemsLayout>
                                            <GridItemsLayout Orientation="Horizontal" Span="1" />
                                        </CollectionView.ItemsLayout>
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate x:DataType="viewmodels:DayCompletionViewModel">
                                                <VerticalStackLayout Spacing="5" Margin="0,0,5,0">
                                                    <Grid HeightRequest="25" HorizontalOptions="Center">
                                                        <!-- Color dot indicator for today -->
                                                        <Ellipse WidthRequest="10"
                                                                 HeightRequest="10"
                                                                 HorizontalOptions="Center"
                                                                 VerticalOptions="Start"
                                                                 IsVisible="{Binding IsToday}">
                                                            <Ellipse.Fill>
                                                                <SolidColorBrush Color="{Binding HabitColor}"/>
                                                            </Ellipse.Fill>
                                                        </Ellipse>
                                                        <Label Text="{Binding DayName}"
                                                               FontSize="12"
                                                               HorizontalOptions="Center"
                                                               VerticalOptions="End"
                                                               TextColor="{StaticResource Gray600}"/>
                                                    </Grid>
                                                    <Frame CornerRadius="20"
                                                           WidthRequest="40"
                                                           HeightRequest="40"
                                                           Padding="0"
                                                           BorderColor="{Binding HabitColor}"
                                                           BackgroundColor="{Binding CompletionBackgroundColor}"
                                                           Opacity="{Binding ShouldTrack, Converter={StaticResource BoolToOpacityConverter}}"
                                                           HasShadow="False">
                                                        <Frame.GestureRecognizers>
                                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=ToggleCompletionCommand}"
                                                                                CommandParameter="{Binding .}"/>
                                                        </Frame.GestureRecognizers>
                                                        <Label Text="✓" 
                                                               IsVisible="{Binding IsCompleted}"
                                                               FontSize="20"
                                                               TextColor="White"
                                                               HorizontalOptions="Center"
                                                               VerticalOptions="Center"/>
                                                    </Frame>
                                                </VerticalStackLayout>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Sectioned View (when ShowArchived is ON) -->
                <VerticalStackLayout Spacing="20" IsVisible="{Binding ShowArchived}">
                    <!-- Active Habits Section -->
                    <VerticalStackLayout Spacing="10">
                        <!-- Section Header -->
                        <HorizontalStackLayout Spacing="10">
                            <Label FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="Green"
                                   VerticalOptions="Center">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0} Habits">
                                        <Binding Path="ActiveHabitsExpanded" Converter="{StaticResource BoolToCollapseIconConverter}" />
                                    </MultiBinding>
                                </Label.Text>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ToggleActiveHabitsSectionCommand}"/>
                                </Label.GestureRecognizers>
                            </Label>
                        </HorizontalStackLayout>

                        <!-- Active Habits List -->
                        <CollectionView ItemsSource="{Binding ActiveHabits}"
                                      SelectionMode="None"
                                      IsVisible="{Binding ActiveHabitsExpanded, Converter={StaticResource InvertedBoolConverter}}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:HabitCardViewModel">
                                    <Frame CornerRadius="15"
                                           BorderColor="{Binding HabitColor}"
                                           HasShadow="True"
                                           Margin="0,0,0,5"
                                           Padding="15">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=EditHabitCommand}"
                                                                CommandParameter="{Binding Id}"/>
                                        </Frame.GestureRecognizers>

                                        <VerticalStackLayout Spacing="8">
                                            <Label Text="{Binding Name}"
                                                   FontSize="20"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding HabitColor}"/>

                                            <Label Text="{Binding Description}"
                                                   FontSize="14"
                                                   TextColor="{StaticResource Gray600}"
                                                   MaxLines="3"
                                                   LineBreakMode="TailTruncation"
                                                   IsVisible="{Binding Description, Converter={StaticResource StringNullOrEmptyConverter}}"/>

                                            <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,-5,0,0">
                                                <Label Grid.Column="0"
                                                       FontSize="14"
                                                       TextColor="{StaticResource Gray600}">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Week " />
                                                            <Span Text="{Binding WeekNumber}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <HorizontalStackLayout Grid.Column="2"
                                                                      Spacing="8"
                                                                      HorizontalOptions="End"
                                                                      Margin="0,0,10,0">
                                                    <controls:CircularProgressView
                                                        Progress="{Binding WeeklyCompletionDecimal}"
                                                        ProgressColor="{Binding HabitColor}"
                                                        TrackColor="{StaticResource Gray300}"
                                                        StrokeWidth="3"
                                                        WidthRequest="34"
                                                        HeightRequest="34"
                                                        VerticalOptions="Center"/>
                                                    <Label Text="{Binding WeeklyCompletionPercentage}"
                                                           FontSize="16"
                                                           FontAttributes="Bold"
                                                           TextColor="{Binding HabitColor}"
                                                           VerticalOptions="Center"/>
                                                </HorizontalStackLayout>
                                            </Grid>

                                            <CollectionView ItemsSource="{Binding WeekDays}"
                                                          SelectionMode="None"
                                                          Margin="0,-10,0,0">
                                                <CollectionView.ItemsLayout>
                                                    <GridItemsLayout Orientation="Horizontal" Span="1" />
                                                </CollectionView.ItemsLayout>
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate x:DataType="viewmodels:DayCompletionViewModel">
                                                        <VerticalStackLayout Spacing="5" Margin="0,0,5,0">
                                                            <Grid HeightRequest="25" HorizontalOptions="Center">
                                                                <Ellipse WidthRequest="10"
                                                                         HeightRequest="10"
                                                                         HorizontalOptions="Center"
                                                                         VerticalOptions="Start"
                                                                         IsVisible="{Binding IsToday}">
                                                                    <Ellipse.Fill>
                                                                        <SolidColorBrush Color="{Binding HabitColor}"/>
                                                                    </Ellipse.Fill>
                                                                </Ellipse>
                                                                <Label Text="{Binding DayName}"
                                                                       FontSize="12"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="End"
                                                                       TextColor="{StaticResource Gray600}"/>
                                                            </Grid>
                                                            <Frame CornerRadius="20"
                                                                   WidthRequest="40"
                                                                   HeightRequest="40"
                                                                   Padding="0"
                                                                   BorderColor="{Binding HabitColor}"
                                                                   BackgroundColor="{Binding CompletionBackgroundColor}"
                                                                   Opacity="{Binding ShouldTrack, Converter={StaticResource BoolToOpacityConverter}}"
                                                                   HasShadow="False">
                                                                <Frame.GestureRecognizers>
                                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=ToggleCompletionCommand}"
                                                                                        CommandParameter="{Binding .}"/>
                                                                </Frame.GestureRecognizers>
                                                                <Label Text="✓"
                                                                       IsVisible="{Binding IsCompleted}"
                                                                       FontSize="20"
                                                                       TextColor="White"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="Center"/>
                                                            </Frame>
                                                        </VerticalStackLayout>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>

                    <!-- Completed Habits Section -->
                    <VerticalStackLayout Spacing="10" IsVisible="{Binding HasCompletedHabits}">
                        <!-- Section Header -->
                        <HorizontalStackLayout Spacing="10">
                            <Label FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Secondary}"
                                   VerticalOptions="Center">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0} Completed">
                                        <Binding Path="CompletedHabitsExpanded" Converter="{StaticResource BoolToCollapseIconConverter}" />
                                    </MultiBinding>
                                </Label.Text>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ToggleCompletedHabitsSectionCommand}"/>
                                </Label.GestureRecognizers>
                            </Label>
                        </HorizontalStackLayout>

                        <!-- Completed Habits List -->
                        <CollectionView ItemsSource="{Binding CompletedHabits}"
                                      SelectionMode="None"
                                      IsVisible="{Binding CompletedHabitsExpanded, Converter={StaticResource InvertedBoolConverter}}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:HabitCardViewModel">
                                    <Frame CornerRadius="15"
                                           BorderColor="{Binding HabitColor}"
                                           HasShadow="True"
                                           Margin="0,0,0,5"
                                           Padding="15">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=EditHabitCommand}"
                                                                CommandParameter="{Binding Id}"/>
                                        </Frame.GestureRecognizers>

                                        <VerticalStackLayout Spacing="8">
                                            <Label Text="{Binding Name}"
                                                   FontSize="20"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding HabitColor}"/>

                                            <Label Text="{Binding Description}"
                                                   FontSize="14"
                                                   TextColor="{StaticResource Gray600}"
                                                   MaxLines="3"
                                                   LineBreakMode="TailTruncation"
                                                   IsVisible="{Binding Description, Converter={StaticResource StringNullOrEmptyConverter}}"/>

                                            <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,-5,0,0">
                                                <Label Grid.Column="0"
                                                       Text="{Binding Deadline, StringFormat='Due Date: {0:MMM dd, yyyy}'}"
                                                       FontSize="13"
                                                       TextColor="{StaticResource Gray600}"
                                                       IsVisible="{Binding Deadline, Converter={StaticResource NullToBoolConverter}}"/>
                                                <HorizontalStackLayout Grid.Column="2"
                                                                      Spacing="8"
                                                                      HorizontalOptions="End"
                                                                      Margin="0,0,10,0">
                                                    <controls:CircularProgressView
                                                        Progress="{Binding WeeklyCompletionDecimal}"
                                                        ProgressColor="{Binding HabitColor}"
                                                        TrackColor="{StaticResource Gray300}"
                                                        StrokeWidth="3"
                                                        WidthRequest="34"
                                                        HeightRequest="34"
                                                        VerticalOptions="Center"/>
                                                    <Label Text="{Binding WeeklyCompletionPercentage}"
                                                           FontSize="16"
                                                           FontAttributes="Bold"
                                                           TextColor="{Binding HabitColor}"
                                                           VerticalOptions="Center"/>
                                                </HorizontalStackLayout>
                                            </Grid>

                                            <CollectionView ItemsSource="{Binding WeekDays}"
                                                          SelectionMode="None"
                                                          Margin="0,-10,0,0">
                                                <CollectionView.ItemsLayout>
                                                    <GridItemsLayout Orientation="Horizontal" Span="1" />
                                                </CollectionView.ItemsLayout>
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate x:DataType="viewmodels:DayCompletionViewModel">
                                                        <VerticalStackLayout Spacing="5" Margin="0,0,5,0">
                                                            <Grid HeightRequest="25" HorizontalOptions="Center">
                                                                <Ellipse WidthRequest="10"
                                                                         HeightRequest="10"
                                                                         HorizontalOptions="Center"
                                                                         VerticalOptions="Start"
                                                                         IsVisible="{Binding IsToday}">
                                                                    <Ellipse.Fill>
                                                                        <SolidColorBrush Color="{Binding HabitColor}"/>
                                                                    </Ellipse.Fill>
                                                                </Ellipse>
                                                                <Label Text="{Binding DayName}"
                                                                       FontSize="12"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="End"
                                                                       TextColor="{StaticResource Gray600}"/>
                                                            </Grid>
                                                            <Frame CornerRadius="20"
                                                                   WidthRequest="40"
                                                                   HeightRequest="40"
                                                                   Padding="0"
                                                                   BorderColor="{Binding HabitColor}"
                                                                   BackgroundColor="{Binding CompletionBackgroundColor}"
                                                                   Opacity="{Binding ShouldTrack, Converter={StaticResource BoolToOpacityConverter}}"
                                                                   HasShadow="False">
                                                                <Frame.GestureRecognizers>
                                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=ToggleCompletionCommand}"
                                                                                        CommandParameter="{Binding .}"/>
                                                                </Frame.GestureRecognizers>
                                                                <Label Text="✓"
                                                                       IsVisible="{Binding IsCompleted}"
                                                                       FontSize="20"
                                                                       TextColor="White"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="Center"/>
                                                            </Frame>
                                                        </VerticalStackLayout>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>

                    <!-- Untracked Habits Section -->
                    <VerticalStackLayout Spacing="10" IsVisible="{Binding HasUntrackedHabits}">
                        <!-- Section Header -->
                        <HorizontalStackLayout Spacing="10">
                            <Label FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#FFC107"
                                   VerticalOptions="Center">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0} Untracked">
                                        <Binding Path="UntrackedHabitsExpanded" Converter="{StaticResource BoolToCollapseIconConverter}" />
                                    </MultiBinding>
                                </Label.Text>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ToggleUntrackedHabitsSectionCommand}"/>
                                </Label.GestureRecognizers>
                            </Label>
                        </HorizontalStackLayout>

                        <!-- Untracked Habits List (using active habit format) -->
                        <CollectionView ItemsSource="{Binding UntrackedHabits}"
                                      SelectionMode="None"
                                      IsVisible="{Binding UntrackedHabitsExpanded, Converter={StaticResource InvertedBoolConverter}}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:HabitCardViewModel">
                                    <Frame CornerRadius="15"
                                           BorderColor="{Binding HabitColor}"
                                           HasShadow="True"
                                           Margin="0,0,0,5"
                                           Padding="15">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=EditHabitCommand}"
                                                                CommandParameter="{Binding Id}"/>
                                        </Frame.GestureRecognizers>

                                        <VerticalStackLayout Spacing="8">
                                            <Label Text="{Binding Name}"
                                                   FontSize="20"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding HabitColor}"/>

                                            <Label Text="{Binding Description}"
                                                   FontSize="14"
                                                   TextColor="{StaticResource Gray600}"
                                                   MaxLines="3"
                                                   LineBreakMode="TailTruncation"
                                                   IsVisible="{Binding Description, Converter={StaticResource StringNullOrEmptyConverter}}"/>

                                            <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,-5,0,0">
                                                <Label Grid.Column="0"
                                                       FontSize="14"
                                                       TextColor="{StaticResource Gray600}">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="Week " />
                                                            <Span Text="{Binding WeekNumber}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <HorizontalStackLayout Grid.Column="2"
                                                                      Spacing="8"
                                                                      HorizontalOptions="End"
                                                                      Margin="0,0,10,0">
                                                    <controls:CircularProgressView
                                                        Progress="{Binding WeeklyCompletionDecimal}"
                                                        ProgressColor="{Binding HabitColor}"
                                                        TrackColor="{StaticResource Gray300}"
                                                        StrokeWidth="3"
                                                        WidthRequest="34"
                                                        HeightRequest="34"
                                                        VerticalOptions="Center"/>
                                                    <Label Text="{Binding WeeklyCompletionPercentage}"
                                                           FontSize="16"
                                                           FontAttributes="Bold"
                                                           TextColor="{Binding HabitColor}"
                                                           VerticalOptions="Center"/>
                                                </HorizontalStackLayout>
                                            </Grid>

                                            <CollectionView ItemsSource="{Binding WeekDays}"
                                                          SelectionMode="None"
                                                          Margin="0,-10,0,0">
                                                <CollectionView.ItemsLayout>
                                                    <GridItemsLayout Orientation="Horizontal" Span="1" />
                                                </CollectionView.ItemsLayout>
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate x:DataType="viewmodels:DayCompletionViewModel">
                                                        <VerticalStackLayout Spacing="5" Margin="0,0,5,0">
                                                            <Grid HeightRequest="25" HorizontalOptions="Center">
                                                                <Ellipse WidthRequest="10"
                                                                         HeightRequest="10"
                                                                         HorizontalOptions="Center"
                                                                         VerticalOptions="Start"
                                                                         IsVisible="{Binding IsToday}">
                                                                    <Ellipse.Fill>
                                                                        <SolidColorBrush Color="{Binding HabitColor}"/>
                                                                    </Ellipse.Fill>
                                                                </Ellipse>
                                                                <Label Text="{Binding DayName}"
                                                                       FontSize="12"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="End"
                                                                       TextColor="{StaticResource Gray600}"/>
                                                            </Grid>
                                                            <Frame CornerRadius="20"
                                                                   WidthRequest="40"
                                                                   HeightRequest="40"
                                                                   Padding="0"
                                                                   BorderColor="{Binding HabitColor}"
                                                                   BackgroundColor="{Binding CompletionBackgroundColor}"
                                                                   Opacity="{Binding ShouldTrack, Converter={StaticResource BoolToOpacityConverter}}"
                                                                   HasShadow="False">
                                                                <Frame.GestureRecognizers>
                                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:HabitViewModel}}, Path=ToggleCompletionCommand}"
                                                                                        CommandParameter="{Binding .}"/>
                                                                </Frame.GestureRecognizers>
                                                                <Label Text="✓"
                                                                       IsVisible="{Binding IsCompleted}"
                                                                       FontSize="20"
                                                                       TextColor="White"
                                                                       HorizontalOptions="Center"
                                                                       VerticalOptions="Center"/>
                                                            </Frame>
                                                        </VerticalStackLayout>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Floating Action Button -->
        <Button Text="+"
                FontSize="30"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,20,20"
                Command="{Binding AddHabitCommand}"/>

        <!-- Popup Overlay -->
        <Grid IsVisible="{Binding ShowOverlay}"
              BackgroundColor="Transparent">
            <!-- Background overlay -->
            <BoxView BackgroundColor="#40000000"
                     HorizontalOptions="Fill"
                     VerticalOptions="Fill">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CloseOverlayCommand}"/>
                </BoxView.GestureRecognizers>
            </BoxView>
            
            <!-- Modal popup container -->
            <Border WidthRequest="400"
                    HeightRequest="380"
                    BackgroundColor="White"
                    StrokeThickness="0"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Shadow="{Shadow Brush=Black, Opacity=0.3, Radius=20, Offset='0,10'}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16"/>
                </Border.StrokeShape>
                <Border.GestureRecognizers>
                    <TapGestureRecognizer/>
                </Border.GestureRecognizers>
                
                <Grid RowDefinitions="Auto,*,Auto" Padding="0">
                    <!-- Header -->
                    <Grid Grid.Row="0" Padding="24,20">
                        <Label Text="Create New" 
                               FontSize="22" 
                               FontAttributes="Bold"
                               TextColor="#212121"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Grid>
                    
                    <!-- Options -->
                    <VerticalStackLayout Grid.Row="1" Spacing="12" Padding="20,0" VerticalOptions="Center">
                        <Border BackgroundColor="#F8F9FA"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                Padding="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="60,*,40" Padding="20,20">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding NavigateToHabitCommand}"/>
                                </Grid.GestureRecognizers>
                                
                                <Frame Grid.Column="0" 
                                       WidthRequest="48" 
                                       HeightRequest="48" 
                                       CornerRadius="24" 
                                       BackgroundColor="#6366F1" 
                                       Padding="0"
                                       HasShadow="False">
                                    <Label Text="🎯" 
                                           FontSize="26" 
                                           HorizontalOptions="Center" 
                                           VerticalOptions="Center"/>
                                </Frame>
                                
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="12,0,0,0">
                                    <Label Text="New Habit" 
                                           FontSize="17" 
                                           FontAttributes="Bold"
                                           TextColor="#212121"/>
                                    <Label Text="Track recurring activities" 
                                           FontSize="13" 
                                           TextColor="#757575"
                                           Margin="0,2,0,0"/>
                                </VerticalStackLayout>
                                
                                <Label Grid.Column="2" 
                                       Text="›" 
                                       FontSize="28" 
                                       TextColor="#BDBDBD"
                                       VerticalOptions="Center"/>
                            </Grid>
                        </Border>
                        <Border BackgroundColor="#F8F9FA"
                                Stroke="#E0E0E0"
                                StrokeThickness="1"
                                Padding="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="60,*,40" Padding="20,20">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding NavigateToTaskCommand}"/>
                                </Grid.GestureRecognizers>
                                
                                <Frame Grid.Column="0" 
                                       WidthRequest="48" 
                                       HeightRequest="48" 
                                       CornerRadius="24" 
                                       BackgroundColor="#10B981" 
                                       Padding="0"
                                       HasShadow="False">
                                    <Label Text="✓" 
                                           FontSize="26" 
                                           HorizontalOptions="Center" 
                                           VerticalOptions="Center"/>
                                </Frame>
                                
                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="12,0,0,0">
                                    <Label Text="New Task" 
                                           FontSize="17" 
                                           FontAttributes="Bold"
                                           TextColor="#212121"/>
                                    <Label Text="One-time to-do items" 
                                           FontSize="13" 
                                           TextColor="#757575"
                                           Margin="0,2,0,0"/>
                                </VerticalStackLayout>
                                
                                <Label Grid.Column="2" 
                                       Text="›" 
                                       FontSize="28" 
                                       TextColor="#BDBDBD"
                                       VerticalOptions="Center"/>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                    
                    <!-- Cancel button at bottom -->
                    <Border Grid.Row="2"
                            Stroke="#E0E0E0"
                            StrokeThickness="1"
                            BackgroundColor="#F5F5F5"
                            Margin="20,0,20,16"
                            Padding="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8"/>
                        </Border.StrokeShape>
                        <Button Text="Cancel"
                                Command="{Binding CloseOverlayCommand}"
                                BackgroundColor="Transparent"
                                TextColor="#757575"
                                FontSize="16"
                                FontAttributes="Bold"
                                Padding="12"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
